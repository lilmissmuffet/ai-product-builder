import { NextResponse } from "next/server";
import { askAI } from "../../../../lib/ai";
import { requireApiUser } from "../../../../lib/auth";
export async function POST(request: Request) { try {await requireApiUser(request);const body=await request.json();if(!body.concept||!body.instruction)throw new Error("A concept and an instruction are required.");const result=await askAI(`You are updating an existing product concept. Apply the user's instruction while preserving useful details and consistency. Do not produce an unrelated concept. Return ONLY valid JSON with keys concept and reply. concept must have exactly productName, productDescription, features (string array), navigation (string array), pages (array of {name,purpose,sections:string array}), designDirection ({palette:string, typography:string, layoutStyle:string, visualTone:string}). reply is one concise explanation of what changed. CURRENT:${JSON.stringify(body.concept)} INSTRUCTION:${body.instruction} CONTEXT:${JSON.stringify(body.context||{})}`);return NextResponse.json(result);}catch(error){const message=error instanceof Error?error.message:"Unable to update product concept.";return NextResponse.json({error:message},{status:message==="Unauthorized"?401:400});}}
